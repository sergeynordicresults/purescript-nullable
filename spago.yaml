package:
  name: nullable
  publish:
    license: MIT
    version: 6.0.0
    location:
      githubOwner: purescript-contrib
      githubRepo: purescript-nullable
  dependencies:
    - functions
    - maybe
    - prelude
  test:
    main: Test.Main
    dependencies:
      - assert
      - effect
workspace:
  extraPackages: {}
  packageSet:
    registry: 60.5.0
